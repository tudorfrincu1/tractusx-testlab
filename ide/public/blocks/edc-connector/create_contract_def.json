{
  "type": "create_contract_def",
  "label": "Create Contract Definition",
  "description": "Create a contract definition linking an asset to a policy.",
  "params": [
    { "name": "service", "type": "service_ref", "required": true, "description": "EDC connector service" },
    { "name": "contract_def_id", "type": "string", "required": false, "description": "Contract definition ID (auto-generated if omitted)" },
    { "name": "access_policy_id", "type": "string", "required": true, "description": "ID of the access policy" },
    { "name": "contract_policy_id", "type": "string", "required": true, "description": "ID of the contract policy" },
    { "name": "asset_selector", "type": "json", "required": true, "description": "Criteria selecting which assets this contract covers" }
  ],
  "outputs": [
    { "name": "contract_def_id", "description": "ID of the created contract definition" }
  ]
}
