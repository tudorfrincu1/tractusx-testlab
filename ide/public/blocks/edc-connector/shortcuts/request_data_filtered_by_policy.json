{
  "type": "request_data_filtered_by_policy",
  "label": "Obtain Data Exchange Authorization by Policy",
  "description": "Query catalog with filters, negotiate a contract matching a specific policy, initiate a PULL transfer, and return the EDR token and dataplane URL.",
  "params": [
    { "name": "service", "type": "service_ref", "required": true, "description": "Consumer EDC connector service" },
    { "name": "counter_party_address", "type": "string", "required": true, "description": "DSP endpoint of the provider connector" },
    { "name": "filters", "type": "filter_expression_list", "required": true, "description": "Filter expressions to narrow catalog results" },
    { "name": "permissions", "type": "array", "item_type": "odrl_permission", "required": true, "description": "ODRL permissions the offer policy must contain" },
    { "name": "prohibitions", "type": "array", "item_type": "odrl_prohibition", "required": false, "description": "ODRL prohibitions the offer policy must contain" },
    { "name": "obligations", "type": "array", "item_type": "odrl_obligation", "required": false, "description": "ODRL obligations the offer policy must contain" },
    { "name": "transfer_type", "type": "dropdown", "required": false, "description": "Transfer type", "options": ["HttpData-PULL", "HttpData-PUSH"], "default": "HttpData-PULL" }
  ],
  "outputs": [
    { "name": "catalog", "description": "Full catalog response object", "schema": { "type": "object" } },
    { "name": "datasets", "description": "Raw catalog datasets matching the filters", "schema": { "type": "array" } },
    { "name": "asset_id", "description": "Resolved asset ID from catalog", "schema": { "type": "string" } },
    { "name": "negotiation_id", "description": "Contract negotiation ID", "schema": { "type": "string" } },
    { "name": "transfer_process_id", "description": "Transfer process ID", "schema": { "type": "string" } },
    { "name": "edr_token", "description": "EDR authorization token for dataplane calls", "schema": { "type": "string" } },
    { "name": "dataplane_url", "description": "Dataplane public API endpoint URL", "schema": { "type": "string" } }
  ]
}
