{
  "type": "schema",
  "uses": "validate/schema",
  "expect": true,
  "sequence": { "type": "validation" },
  "label": "Validate Schema",
  "description": "Validate that an object conforms to a JSON schema definition.",
  "params": [
    { "name": "input", "type": "class", "required": true, "description": "The object to validate against the schema", "accepts": ["*"] },
    { "name": "schema", "type": "text", "required": true, "description": "Reference to the JSON schema (e.g., from env.schemas)" }
  ],
  "outputs": []
}
