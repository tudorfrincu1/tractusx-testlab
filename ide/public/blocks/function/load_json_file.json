{
  "type": "load_json_file",
  "label": "Load JSON File",
  "description": "Load a JSON file from the test resources directory.",
  "params": [
    { "name": "path", "type": "string", "required": true, "description": "Relative path to the JSON file" }
  ],
  "outputs": [
    { "name": "data", "description": "Parsed JSON content" }
  ]
}
