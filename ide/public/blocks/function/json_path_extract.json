{
  "type": "json_path_extract",
  "label": "Extract JSON Path",
  "description": "Extract a value from a JSON variable using JSONPath.",
  "params": [
    { "name": "variable", "type": "variable", "required": true, "description": "Source variable containing JSON" },
    { "name": "path", "type": "string", "required": true, "description": "JSONPath expression (e.g., items[0].id)" }
  ],
  "outputs": [
    { "name": "value", "description": "Extracted value" }
  ]
}
