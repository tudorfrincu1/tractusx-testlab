{
  "type": "extract_dataset",
  "label": "Extract Dataset",
  "description": "Extract a specific dataset from a catalog response by filtering on dct:type.",
  "params": [
    { "name": "variable", "type": "variable", "required": true, "description": "Source variable containing the catalog response" },
    { "name": "dct_type", "type": "string", "required": true, "description": "Dataset type filter (e.g., https://w3id.org/catenax/taxonomy#DigitalTwinRegistry)" }
  ],
  "outputs": [
    { "name": "dataset", "description": "The matched dataset object" },
    { "name": "asset_id", "description": "Asset ID of the matched dataset" },
    { "name": "policies", "description": "Usage policies attached to the dataset" }
  ]
}
