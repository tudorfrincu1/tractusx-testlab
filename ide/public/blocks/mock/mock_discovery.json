{
  "type": "mock_discovery",
  "label": "Mock Discovery Service",
  "description": "Expose a discovery service mock that returns configured BPN-to-EDC endpoint mappings.",
  "template": true,
  "params": [
    { "name": "id", "type": "string", "required": true, "description": "Unique mock identifier" },
    { "name": "mappings", "type": "json", "required": true, "description": "BPN → EDC endpoint mappings to return" }
  ]
}
