################################################################################
# Eclipse Tractus-X - Tractus-X TestLab
#
# Copyright (c) 2026 Contributors to the Eclipse Foundation
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License, Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0.
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.
#
# SPDX-License-Identifier: Apache-2.0
################################################################################
## This code was partially generated using artificial intelligence (AI) (Tool: Copilot, Model: Claude Opus 4.6).
## It was reviewed and tested by a human committer.

kind: test
name: error-handling
version: "1.0"
dataspace_version: "saturn"
description: >
  Verify CX-0135 error handling: trigger a REJECTED status response from
  the SUT and validate that the response envelope follows the standard
  header + content structure with requestStatus = REJECTED.

services:
  - name: provider_edc
    type: connector_consumer
    base_url: "@provider_address"

steps:
  # ── Catalog discovery ──────────────────────────────────────────────
  - type: query_catalog_with_filters
    description: Discover CCMAPI offer for error handling test
    params:
      service: provider_edc
      counter_party_address: "@provider_address"
      filter:
        filter_expression:
          - operand_left: "https://w3id.org/edc/v0.0.1/ns/type"
            operator: "="
            operand_right: "https://w3id.org/catenax/taxonomy#CCMAPI"
          - operand_left: "http://purl.org/dc/terms/subject"
            operator: "="
            operand_right: "https://w3id.org/catenax/taxonomy#CompanyCertificateManagementNotificationApi"
          - operand_left: "https://w3id.org/catenax/ontology/common#version"
            operator: "="
            operand_right: "3.0"
    expect:
      - type: "NOT_NULL"
        output: datasets

  # ── Extract catalog fields ─────────────────────────────────────────
  - type: json_path_extract
    description: Extract asset ID from catalog
    params:
      variable: "datasets"
      path: "[0].@id"
      store_in_variable: err_asset_id

  - type: json_path_extract
    description: Extract offer policy from catalog
    params:
      variable: "datasets"
      path: "[0].odrl:hasPolicy"
      store_in_variable: err_offer_id

  # ── Contract negotiation ───────────────────────────────────────────
  - type: negotiate
    description: Negotiate contract for error handling test
    params:
      service: provider_edc
      counter_party_address: "@provider_address"
      offer_id: "@err_offer_id"
      asset_id: "@err_asset_id"
      store_in_variable: err_agreement_id

  # ── Transfer / EDR ─────────────────────────────────────────────────
  - type: initiate_transfer
    description: Get data plane credentials
    params:
      service: provider_edc
      agreement_id: "@err_agreement_id"
      asset_id: "@err_asset_id"
      transfer_type: "HttpData-PULL"

  # ── UUID for this test ─────────────────────────────────────────────
  - type: generate_uuid
    description: Generate message ID for rejected request
    params:
      store_in_variable: err_message_id

  # ── Send request with reject flag ──────────────────────────────────
  - type: http_call_dataplane
    description: >
      Send certificate request to the REJECTED endpoint to verify
      CX-0135 error handling — SUT must return header + content with
      requestStatus = REJECTED (section 2.1.1.1.4)
    params:
      method: POST
      url: "@data_address/companycertificate/request?reject=true"
      edr_token: "@edr_token"
      headers:
        Content-Type: "application/json"
      body:
        header:
          messageId: "urn:uuid:@err_message_id"
          context: "CompanyCertificateManagement-CCMAPI-Request:1.0.0"
          sentDateTime: "2026-01-01T00:00:00Z"
          senderBpn: "@consumer_bpn"
          receiverBpn: "@provider_bpn"
          relatedMessageId: "urn:uuid:@err_message_id"
          version: "3.1.0"
          senderFeedbackUrl: "@testlab_dsp_url"
        content:
          certifiedBpn: "@provider_bpn"
          certificateType: "iso9001"
          locationBpns:
            - "@location_bpns"
    expect:
      - type: "ASSERT_FIELD"
        output: response_body
        path: "header.messageId"
        operator: "not_null"
      - type: "ASSERT_FIELD"
        output: response_body
        path: "header.context"
        operator: "eq"
        value: "CompanyCertificateManagement-CCMAPI-Request:1.0.0"
      - type: "ASSERT_FIELD"
        output: response_body
        path: "content.requestStatus"
        operator: "eq"
        value: "REJECTED"
      - type: "ASSERT_FIELD"
        output: response_body
        path: "content.documentId"
        operator: "not_null"
